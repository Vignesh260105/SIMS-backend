services:
  - type: web
    name: sims-backend
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/student-internship-management-0.0.1-SNAPSHOT.jar
    envVars:
      - key: DATABASE_URL
        fromSecret: database_url  # Assuming you set this in Render secrets